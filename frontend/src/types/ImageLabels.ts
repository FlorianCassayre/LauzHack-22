export interface ImageLabels {
    // Unused
    columns: any;
    // Unused
    index: any;
    data: [xmin: number, ymin: number, xmax: number, ymax: number, confidence: number, clazz: number, name: string][];
}
